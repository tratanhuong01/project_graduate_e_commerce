export const addComment = (data) => {
  return async (dispatch) => {
    // const comment = {
    //   id: 1,
    //   commentUser: data.user,
    //   fullName: data.user !== null ? null : data.fullName,
    //   email: data.user !== null ? null : data.email,
    //   level: 1,
    //   reply: null,
    //   commentNews: null,
    //   content: data.content,
    //   timeCreated: "",
    // };
    if (data.user) {
    } else {
    }
  };
};
