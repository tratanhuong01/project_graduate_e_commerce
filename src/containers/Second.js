import React from "react";
import { useSelector } from "react-redux";

function Second(props) {
  //
  //
  return (
    <div
      className={`w-full h-screen bg-opacity-50 bg-black top-0 left-0 fixed z-50 hidden`}
    ></div>
  );
}

export default Second;
